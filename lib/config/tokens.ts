// Defining the constant tokenTypes
export const tokenTypes = {
  VERIFY_EMAIL: "verifyEmail", // Token type used for email verification
  REGISTER_NEW_DEVICE: "registerNewDevice", // Token type used for registering a new device
};
